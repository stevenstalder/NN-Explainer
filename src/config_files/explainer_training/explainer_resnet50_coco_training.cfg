classifier_type = resnet50
data_base_path = ../datasets/
dataset = COCO
early_stop_patience=2
entropy_regularizer = 3.0
learning_rate = 1e-05
classifier_checkpoint = <path to a .ckpt file containing a pretrained resnet50 classifier on COCO>
mask_total_area_regularizer = 0.01
mask_variation_regularizer = 0.05
ncmask_total_area_regularizer = 0.03
mask_area_constraint_regularizer = 0.1
model_to_train = explainer

