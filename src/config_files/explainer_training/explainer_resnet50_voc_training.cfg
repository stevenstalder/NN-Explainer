classifier_type = resnet50
data_base_path = ../datasets/
dataset = VOC
entropy_regularizer = 3.0
learning_rate = 1e-04
classifier_checkpoint = <path to a .ckpt file containing a pretrained resnet50 classifier on VOC>
mask_area_constraint_regularizer = 0.8
mask_total_area_regularizer = 0.01
model_to_train = explainer
