classifier_type = vgg16
data_base_path = ../datasets/
dataset = VOC
learning_rate = 1e-05
classifier_checkpoint = <path to a .ckpt file containing a pretrained vgg16 classifier on VOC>
model_to_train = explainer
